include #sockets
include #system

// this comment is for the socket class
class Socket
{
   /* This is a comment on the Connect method 
    * that spans for a few lines
    */
   void Connect(string server)
   {
      SocketLibrary.Connect(mSocket, server);
   }

   void Disconnect()
   {
      SocketLibrary.Disconnect(mSocket);
   }

   // this comment goes at the end
   // of the file, who is its owner?
}